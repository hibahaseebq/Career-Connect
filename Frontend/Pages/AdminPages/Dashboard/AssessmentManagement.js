import React from 'react';
import { Outlet } from 'react-router-dom';
const AssessmentManagement = ({ token }) => {
  

  return (
      <Outlet/>
  );
};

export default AssessmentManagement;
